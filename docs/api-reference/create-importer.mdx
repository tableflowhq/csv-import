---
title: "Create Importer"
openapi: "POST /importer"
---

### Example Request

```json
{
  "name": "API Test Importer",
  "template": {
    "columns": [
      {
        "name": "First Name",
        "key": "first_name",
        "required": false,
        "description": "The first name of the user",
        "suggested_mappings": [
          "First Name",
          "Name"
        ]
      },
      {
        "name": "Last Name"
      },
      {
        "name": "Email",
        "required": true,
        "description": "The email of the user"
      }
    ]
  }
}
```
